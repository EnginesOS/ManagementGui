module Shared
  module AlertDialogsHelper

    # def alert_dialog(body=nil, opts={})
    #   opts.merge!{footer_close: opts[:footer_close] == false, footer_ok: opts[:footer_ok] == true}
    #   modal(body, opts)
    # end

  end
end
