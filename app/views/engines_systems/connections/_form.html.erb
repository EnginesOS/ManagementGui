<%= form_for @connection, url: system_connection_path(engines_system_id: @engines_system.id), method: :patch do |f| %>
<%= f.engines_field :url, as: :string, label: :URL %>
<%= f.engines_cancel_submit url: system_connection_menu_path(engines_system_id: @engines_system.id), remote: true %>
<% end %>
