<%= custom_form_for @shutdown, url: system_shutdown_path(engines_system_id: @engines_system.id) do |f| %>
<%= f.engines_field :reason, as: :text, left: 2, width: 8 %>
<%= f.engines_cancel_submit %>
<% end %>
