<%= wait_for_local_system_modal( @engines_system,
      { message: "#{@engines_system.label} base OS (#{@engines_system.base_system_version[:name]}) is updating. This normally takes a few minutes.",
        icon: 'fa-refresh' }) %>
