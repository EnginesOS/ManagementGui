<%= custom_form_for @engines_system, url: system_properties_path(engines_system_id: @engines_system.id) do |f| %>
<%= f.engines_field :label %>
<%= f.engines_field :url %>
<%= f.engines_cancel_submit url: system_menu_path(engines_system_id: @engines_system.id), remote: true %>
<% end %>
