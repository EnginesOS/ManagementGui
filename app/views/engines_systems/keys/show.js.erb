$(".modal").modal('hide');
$("body").append("<%= j system_key_menu_modal(@engines_system ) %>");
$("#remote_modal").modal("show");
$("#remote_modal").on("hidden.bs.modal", function(){$(this).remove();});
$("#remote_modal_flash_messages").html("<%= j flash_messages %>");
$("#remote_modal_flash_messages").hide().fadeIn();
