<%= wait_for_local_system_modal( @engines_system,
      { message: "#{@engines_system.label} is updating. Update normally takes a minute or two.",
        icon: 'fa-refresh' }) %>
<!-- close system event stream, otherwise comms error while system is restarting -->
