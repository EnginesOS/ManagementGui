<% @clouds.each do |cloud| %>
  <%= render 'portals/cloud', cloud: cloud %>
<% end if @clouds %>
<% # = render 'portals/user_portal' if current_user && controller_path == 'clouds/portals' %>
<% @apps.each do |app| %>
  <%= render 'portals/app', app: app %>
<% end if @apps %>

<% if @show_sign_in %>
  <script>
  $('#user_sign_in_modal_link').click();
  </script>
<% end %>
