<%= form_for @side_load, url: install_side_load_path(engines_system_id: @engines_system.id ), remote: true do |f| %>
  <%= f.engines_field :install_metadata, as: :hidden %>
  <%= f.engines_field :repository_url, as: :string %>
  <%= f.engines_cancel_submit url: system_control_panel_path(engines_system_id: @engines_system.id ), remote: true %>
<% end %>
