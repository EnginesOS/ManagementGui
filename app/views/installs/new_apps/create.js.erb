$(".modal").modal('hide');
show_wait_for_system_response_spinner();
remote_get("<%= install_build_path(engines_system_id: @engines_system.id) %>");
remote_get("<%= cloud_system_path(engines_system_id: @engines_system.id) %>");
