$(".modal").modal('hide');
$("body").append("<%= j render('apps/menus/show') %>");
$("#container_<%= @app.name %>_menu_modal").modal("show");
$("#container_<%= @app.name %>_menu_modal").on("hidden.bs.modal", function(){$(this).remove();});
$("#container_<%= @app.name %>_menu_modal_flash_messages").html("<%= j(flash_messages) %>");
$("#container_<%= @app.name %>_menu_modal_flash_messages").hide().fadeIn();
