<%= modal footer_close: true, header: {text: "Control panel for #{@app.name}", icon: 'fa-cogs'} do %>
  <%= up_to_app_menu_link(@app) %>
  <%= resource_link :app_service_consumers, params: {app_id: @app.id}, text: 'Services', icon: 'fa-compass', title: "Service connections for #{@app.name}" %>
  <%= resource_link :app_actions, params: {app_id: @app.id}, text: 'Actions', icon: 'fa-crosshairs', title: "Actions for #{@app.name}" %>
  <hr>
  <%= edit_resource_link :app_network, params: {app_id: @app.id}, text: 'Network', icon: 'fa-sitemap', title: "Network properties for #{@app.name}" %>
  <%= edit_resource_link :app_memory, params: {app_id: @app.id}, text: 'Memory', icon: 'fa-pie-chart', title: "Memory settings for #{@app.name}" %>
  <%= resource_link :app_environment, params: {app_id: @app.id}, text: 'Environment', icon: 'fa-question-circle-o', title: "Environment variables for #{@app.name}" %>
  <hr>
  <%= resource_link :app_processes, params: {app_id: @app.id}, text: 'Processes', icon: 'fa-list-alt', title: "Processes for #{@app.name}" %>
  <%= resource_link :app_logs, params: {app_id: @app.id}, text: 'Logs', icon: 'fa-file-text-o', title: "Logs for #{@app.name}" %>
  <%= resource_link :app_report, params: {app_id: @app.id}, text: 'Report', icon: 'fa-stethoscope', title: "Report for #{@app.name}" %>
  <%= resource_link :app_installation_report, params: {app_id: @app.id}, text: 'Installation report', icon: 'fa-list-ol', title: "Installation report for #{@app.name}" %>
  <% # = resource_link :app_about, params: {app_id: @app.id}, text: 'About', icon: 'fa-info-circle', title: "About #{@app.name}" %>
  <hr>
  <%= resource_link :app_reinstall, params: {app_id: @app.id}, text: 'Reinstall', icon: 'fa-plus', title: "Reinstall #{@app.name}" %>
  <%= resource_link :new_app_uninstall, params: {app_id: @app.id}, text: 'Uninstall', icon: 'fa-minus-square', title: "Uninstall #{@app.name}" %>
<% end %>
